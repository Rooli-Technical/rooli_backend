<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <title>Organization Invitation</title>
    <style>
      body {
        font-family: Arial, sans-serif;
        background-color: #f9fafb;
        padding: 20px;
        color: #111827;
      }
      .container {
        max-width: 600px;
        margin: 0 auto;
        background: #ffffff;
        border-radius: 8px;
        padding: 24px;
        box-shadow: 0 2px 6px rgba(0,0,0,0.08);
      }
      h2 {
        color: #1f2937;
      }
      p {
        line-height: 1.6;
      }
      .button {
        display: inline-block;
        margin-top: 20px;
        padding: 12px 20px;
        background: #2563eb;
        color: white;
        text-decoration: none;
        border-radius: 6px;
        font-weight: bold;
      }
      .footer {
        margin-top: 30px;
        font-size: 12px;
        color: #6b7280;
        text-align: center;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h2>
        {{#if isWorkspaceInvite}}
          You’re Invited to the {{contextName}} Workspace
        {{else}}
          You’re Invited to Join {{contextName}}
        {{/if}}
      </h2>

      <p>Hello,</p>

      <p>
        <strong>{{inviterName}}</strong> has invited you to join 
        {{#if isWorkspaceInvite}}
          the <strong>{{contextName}}</strong> workspace 
        {{else}}
          the <strong>{{contextName}}</strong> organization 
        {{/if}}
        as a <strong>{{roleName}}</strong>.
      </p>

      {{#if message}}
        <p style="padding: 12px; background: #f3f4f6; border-left: 4px solid #2563eb; font-style: italic;">
          "{{message}}"
        </p>
      {{/if}}

      <p>Click the button below to accept. This invitation expires in 7 days.</p>

      <a href="{{invitationUrl}}" class="button">Accept Invitation</a>

      <div class="footer">
        <p>If you weren’t expecting this email, you can ignore it.</p>
        <p>© {{year}} Rooli. All rights reserved.</p>
      </div>
    </div>
  </body>
</html>
